// Parsing error: Invalid identifier name. We cannot use a reserved keyword as an identifier name.

package error7

func main() {
    var if = "error" // cannot use reserved keyword as identifiers
    println(if)
    return 
}
